package 线程;

public class 线程方法 {
    public static void main(String[] args) {
        //获取当前线程的名称

        //1.interrupt （中断线程休眠）

        //2.yield（线程的让步，让出cpu，让其他线程执行，但礼让的时间不确定，所以也不一定礼让成功）

        //3.join（线程的插队，插队的线程一旦插队成功，则肯定先执行完插入的线程所有的任务）

        //4.

    }
}
